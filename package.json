{
  "name": "ntn-gateway",
  "version": "2.2.0",
  "description": "Agent-native Notion gateway for durable shared state and human/agent workflow coordination.",
  "main": "src/index.js",
  "bin": {
    "ntn-gateway": "bin/ntn-gateway.js"
  },
  "scripts": {
    "build": "npm run check && node scripts/install-agent-skill-symlinks.js",
    "test": "node --test",
    "check": "node --check bin/ntn-gateway.js && node --check src/*.js && node --check scripts/*.js && node --check test/*.test.js"
  },
  "keywords": [
    "notion",
    "gateway",
    "cli",
    "agent",
    "collaboration"
  ],
  "author": "",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fyang0507/ntn-gateway.git"
  },
  "bugs": {
    "url": "https://github.com/fyang0507/ntn-gateway/issues"
  },
  "homepage": "https://github.com/fyang0507/ntn-gateway#readme",
  "type": "commonjs",
  "dependencies": {
    "@notionhq/client": "^5.22.0",
    "dotenv": "^16.4.7"
  },
  "engines": {
    "node": ">=20"
  }
}
