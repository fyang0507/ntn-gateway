{
  "name": "ntn-gateway",
  "version": "2.0.0",
  "description": "Gateway-scoped Notion operations CLI for agent collaboration.",
  "main": "src/index.js",
  "bin": {
    "ntn-gateway": "bin/ntn-gateway.js"
  },
  "scripts": {
    "build": "npm run check && node scripts/sync-skills.js",
    "test": "node --test",
    "check": "node --check bin/ntn-gateway.js && node --check src/*.js && node --check scripts/*.js && node --check test/*.test.js"
  },
  "keywords": [
    "notion",
    "gateway",
    "cli"
  ],
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "type": "commonjs",
  "dependencies": {
    "@notionhq/client": "^5.22.0",
    "dotenv": "^16.4.7"
  },
  "engines": {
    "node": ">=20"
  }
}
